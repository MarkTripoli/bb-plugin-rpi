Artifact saved:
{artifact_directive}

The implementation outline is ready for review. Check the phase order and validation before starting worktree setup.

Auto-advance does not apply at this human gate. Continue manually with:

```text
/rpi-setup-worktree @{artifact_file}
```
